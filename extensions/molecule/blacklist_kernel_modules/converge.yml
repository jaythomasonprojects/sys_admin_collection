---
- name: Converge blacklist_kernel_modules scenario
  hosts: all
  become: true
  gather_facts: true
  vars:
    blacklist_kernel_modules_disable_usb_storage: >-
      {{ inventory_hostname == 'ubuntu-enabled' }}
  roles:
    - role: 'jaythomasonprojects.sys_admin.blacklist_kernel_modules'
