---
- name: Verify config_power rejection scenario
  hosts: all
  gather_facts: false
  tasks:
    - name: Assert Docker scenario requires no supported-path verification
      ansible.builtin.assert:
        that:
          - true
