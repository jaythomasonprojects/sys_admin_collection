---
- name: Converge config_firewall scenario
  hosts: all
  gather_facts: true
  tasks:
    - name: Reject Ubuntu hosts
      block:
        - name: Configure firewall on Ubuntu
          ansible.builtin.include_role:
            name: 'jaythomasonprojects.sys_admin.config_firewall'
      rescue:
        - name: Record Ubuntu rejection
          ansible.builtin.set_fact:
            config_firewall_ubuntu_error: >-
              {{ ansible_failed_result.msg | default('') }}

    - name: Assert Ubuntu is rejected
      ansible.builtin.assert:
        that:
          - >-
            config_firewall_ubuntu_error | default('') ==
            'config_firewall supports Windows hosts only.'
